#include<stdio.h>
int fact(int n){
    int result=1;
    for(int i=1;i<=n;i++){
            result=result*i;
    }
    return result;
}
int main()
{
    int num,factorial;
    scanf("%d",&num);
    factorial=fact(num);
    printf("%d!=%d\n",num,factorial);
    return 0;
}
