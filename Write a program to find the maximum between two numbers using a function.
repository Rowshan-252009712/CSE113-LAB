#include <stdio.h>
int max(int x,int y)
{   if(x>y){
     return x;
     }
else{
     return y;
    }
}
int main(){
    int a, b, result;
    printf("Enter Numbers: ");
    scanf("%d %d",&a,&b);
    result=max(a,b);
    printf("%d is the maximum number",result);
    return 0;
}
