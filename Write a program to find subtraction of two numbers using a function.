#include <stdio.h>
int sub(int x,int y)
{   int result = x-y;
    return result;
}
int main(){
    int a,b,result;
    printf("Enter Numbers: ");
    scanf("%d %d",&a,&b);
    result = sub(a,b);

    printf("Subtraction of %d & %d : %d",a,b,result);
    return 0;
}
