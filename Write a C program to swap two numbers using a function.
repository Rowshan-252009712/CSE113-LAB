
#include <stdio.h>
int swap(int a, int b) {
int temp=a;
a=b;
b=temp;
printf("After swapping: a=%d, b=%d.",a,b) ;
}
int main(){
int x,y;
printf("Enter a =");
scanf ("%d",&x);
printf("Enter b =");
scanf ("%d",&y) ;
swap(x,y);
return 0;
}
